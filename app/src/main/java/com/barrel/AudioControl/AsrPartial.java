package com.barrel.AudioControl;

import java.util.HashMap;

//类名可以随便起
public class AsrPartial {
    //变量名要跟json中的key对应
    public String results_recognition[];    //这里json 带[]所以是数组
    //由于第一项其实就是识别的结果，可以直接使用了，后面无需多设定
}
